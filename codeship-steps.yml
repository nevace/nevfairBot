- type: serial
  steps:
    - name: test
      service: app
      command: npm run test-verbose
    - name: deploy
      service: ssh
      command: mkdir -p "$HOME/.ssh" && \ ssh-keyscan -t rsa 139.59.178.69 > ~/.ssh/known_hosts && \ echo $PRIVATE_SSH_KEY >> $HOME/.ssh/id_rsa && \ chmod -R 700 $HOME/.ssh && \ cat $HOME/.ssh/id_rsa && \ ssh nevace@139.59.178.69 touch hi4.txt