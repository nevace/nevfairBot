app:
  build:
    image: nevace/nevfairbot
    dockerfile: Dockerfile
  links:
    - mongo
  cached: true
mongo:
  image: mongo
  volumes:
    - /data/db:/data/db
  ports:
    - "27017:27017"
  command: sleep 1