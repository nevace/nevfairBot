- type: serial
  steps:
    - name: test
      service: app
      command: npm run test-verbose
    - name: dockerhub_push
      service: app
      type: push
      image_name: nevace/nevfairbot
      registry: https://index.docker.io/v1/
      encrypted_dockercfg_path: dockercfg.encrypted
    - name: deploy
      service: ssh
      command: ssh nevace@139.59.178.69 'bash -s' < deploy.sh